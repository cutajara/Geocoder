sudo yum update -y
sudo yum install -y docker
sudo service docker start
sudo usermod -a -G docker ec2-user
sudo yum install -y git


sudo su - ec2-user
git clone https://github.com/cutajara/Geocoder.git
cd Geocoder
docker build -t geocoder .
docker run -p 8000:8000 -e AWS_DEFAULT_REGION=ap-southeast-2 geocoder